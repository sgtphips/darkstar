<%= form_with(model: audio, local: true) do |form| %>
  <% if audio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(audio.errors.count, "error") %> prohibited this audio from being saved:</h2>

      <ul>
      <% audio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :audio_title %>
  </div>

  <div class="field">
    <%= form.label :audio %>
    <%= form.file_field :audio, id: :audio_audio %><br>
    <%= @audio.audio %>
  </div>

  <div class="field">
    <%= form.label :created_at %>
    <%= form.datetime_select :created_at, id: :audio_created_at %>
  </div>

  <div class="field">
    <%= form.label :updated_at %>
    <%= form.datetime_select :updated_at, id: :audio_updated_at %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
